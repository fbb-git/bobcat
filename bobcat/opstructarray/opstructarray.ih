// this file is included by arg.ih and arconfig.ih. It is not a
// header file that should be added to the <bobcat/...> files

namespace FBB
{

class OptStructArray
{
    typedef struct option  OptStruct;

    size_t    d_n;
    OptStruct *d_opt;
    
    public:
        OptStructArray(size_t n);
        ~OptStructArray();

        OptStruct *get();
};

#include "opstructarray.i"
#include "destructor.i"
#include "get.i"

} // FBB

