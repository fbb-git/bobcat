// this file is included by arg.ih and arconfig.ih. It is an IUO header
// It is not used elsewhere, and not part of the <bobcat/...> headerset

#include <cstddef>
#include <getopt.h>

namespace FBB
{

class OptStructArray
{
    typedef struct option  OptStruct;

    size_t    d_n;
    OptStruct *d_opt;
    
    public:
        OptStructArray(size_t n);
        ~OptStructArray();

        OptStruct *get();
};

#include "destructor.i"
#include "get.i"

} // FBB

using namespace FBB;

