void uninstall(string logfile)
{
    int idx;
    list entry;
    string dir;
    list line;

    if (!exists(logfile))
    {
        printf("installation log file " + logfile + " not found\n");
        exit(0);
    }

    backtick("icmake/remove " + logfile);

    exit(0);
}







