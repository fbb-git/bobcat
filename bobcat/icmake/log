void log(string file)
{
    string md5;

    if (g_logPath == "")            // do not store uninstall info
        return;

//    file = cutEoln(backtick("realpath " + file)[0]);

    md5 = md5sum(file = cutEoln(file));

    g_log += (list)("file " + md5 + " " + file);
}

