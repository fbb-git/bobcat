void remove(list entry)
{
    int equal;
    string file;

    file = file;

    if (!exists(file))
        return;

    equal = md5sum(file) == entry[1];

    if (equal)
        run("rm " + file);
    else
        printf("not removing modified file ", file, "\n");
}
