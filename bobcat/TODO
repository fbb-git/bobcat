The Wrap classes need additional functionality accepting pointers to context
as well. E.g., Wrap1c should add/change:

        ... (as is)

        union
        {
            ...
// NEW
            ReturnType (*d_refp)(Type &, Context *);
            ReturnType (*d_refconstp)(Type &, Context const *);

            ReturnType (*d_constrefp)(Type const &, Context *);
            ReturnType (*d_constrefconstp)(Type const &, Context const *);

            ReturnType (*d_ptrp)(Type *, Context *);
            ReturnType (*d_ptrconstp)(Type *, Context const *);

            ReturnType (*d_constptrp)(Type const *, Context *);
            ReturnType (*d_constptrconstp)(Type const *, Context const *);

        };

        ...

            // The following additional constructors are identical to the
            // constructors listed above, but they accept a pointer to a 
            // context in various forms.

                                                // reference
            Wrap1c(ReturnType (*fun)(Type &, Context *), Context *context)
            :
                d_context(context),
                d_refp(fun)
            {}
                                                // reference const
            Wrap1c(ReturnType (*fun)(Type &, Context const *), 
                   Context const *context)
            :
                d_contextconst(context),
                d_refconstp(fun)
            {}

                                                // const reference
            Wrap1c(ReturnType (*fun)(Type const &, Context *), 
                   Context *context)
            :
                d_context(context),
                d_constrefp(fun)
            {}
                                                // const reference const
            Wrap1c(ReturnType (*fun)(Type const &, Context const *), 
                   Context const *context)
            :
                d_contextconst(context),
                d_constrefconstp(fun)
            {}

                                                // pointer
            Wrap1c(ReturnType (*fun)(Type *, Context *), 
                   Context *context)
            :
                d_context(context),
                d_ptrp(fun)
            {}
                                                // pointer const
            Wrap1c(ReturnType (*fun)(Type *, Context const *), 
                   Context const *context)
            :
                d_context(context),
                d_ptrconstp(fun)
            {}
                                                // const pointer
            Wrap1c(ReturnType (*fun)(Type const *, Context *), 
                   Context *context)
            :
                d_context(context),
                d_constptrp(fun)
            {}
                                                // const pointer const
            Wrap1c(ReturnType (*fun)(Type const *, Context const *), 
                   Context const *context)
            :
                d_context(context),
                d_constptrconstp(fun)
            {}

            ...
Also inspect Wrap1, Wrap2 and Wrap2c.
The man-page(s) should be upgraded accordingly.



Some manpages contain `itemization' or other yodl-related stuff. Repair.

--

