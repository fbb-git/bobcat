#ifndef INCLUDED_BOBCAT_RANGER_
#define INCLUDED_BOBCAT_RANGER_


namespace FBB
{

template <typename Iter>
class Ranger
{
    Iter d_begin;
    Iter d_end;

    public:
        Ranger(Iter const &begin, Iter const &end);     // .i

        Iter const &begin() const;                      // .i
        Iter const &end() const;                        // .i
};

#include "ranger.if"
#include "begin.if"
#include "end.if"

    // Free functions

#include "ranger1.if"        // ranger(Iter &&begin, Iter &&end)
#include "ranger2.if"        // ranger(Data *begin, Data *end)
#include "ranger3.if"        // ranger(Iter &&begin, size_t count)
#include "ranger4.if"        // ranger(Data *begin, size_t count)

} // FBB

#endif



