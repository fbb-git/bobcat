COPTS="-I../tmp -Wall -O3"
    # compiler options
LIBRARIES=""
    # libraries required by the shared object library. Provide the libraries
    # as blank delimited "-l libbasename" series
LIBRARY="bobcat"
    # static library base name (lib${LIBRARY}.a
GCC="g++-4.0"
    # compiler to use
