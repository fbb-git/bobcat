void addClasses(string filename)
{
    list class;
    string elem;

    while (sizeof(class = fgets(filename, (int)class[1])))
    {
        elem = element(0, strtok(class[0], " \t\n"));
        if (elem != "#")
            g_classes += (list)elem;
    }
}

